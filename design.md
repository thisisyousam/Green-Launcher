# GreenLauncher 디자인 스펙

## 컨셉

심플, 절제된 톤. 라임 컬러 하나만 강조색으로 쓰고 나머지는 무채색으로 눌러준다.
장식 요소 최소화 — 버튼, 진행바, 카드 정도로 구성되는 마인크래프트 런처답게
"조용한 도구" 느낌을 낸다. 화려한 그라데이션이나 그림자 남발 금지.

## 컬러 토큰

### 라이트 모드

| 토큰               | 값        | 용도                      |
| ------------------ | --------- | ------------------------- |
| `--bg`             | `#F7F8F5` | 배경                      |
| `--surface`        | `#FFFFFF` | 카드, 패널                |
| `--border`         | `#E2E5DD` | 구분선, 테두리            |
| `--text-primary`   | `#1C1E1A` | 본문 텍스트               |
| `--text-secondary` | `#6B7268` | 보조 텍스트               |
| `--accent`         | `#8BC53F` | 라임 (버튼, 강조, 포커스) |
| `--accent-hover`   | `#79AF35` | 라임 호버                 |
| `--accent-text`    | `#1C2410` | 라임 배경 위 텍스트       |

### 다크 모드

| 토큰               | 값        | 용도                      |
| ------------------ | --------- | ------------------------- |
| `--bg`             | `#14160F` | 배경                      |
| `--surface`        | `#1D2018` | 카드, 패널                |
| `--border`         | `#2C3024` | 구분선, 테두리            |
| `--text-primary`   | `#EDEFE8` | 본문 텍스트               |
| `--text-secondary` | `#9CA394` | 보조 텍스트               |
| `--accent`         | `#A3D65C` | 라임 (다크에서 살짝 밝게) |
| `--accent-hover`   | `#B4E272` | 라임 호버                 |
| `--accent-text`    | `#14180B` | 라임 배경 위 텍스트       |

**규칙**: 라임은 오직 하나 — 기본 액션(플레이 버튼, 진행바 채움, 활성 탭 인디케이터)에만 쓴다.
아이콘, 보조 버튼, 카드 테두리 등 나머지는 전부 무채색 스케일로 처리해서
라임이 나타났을 때 시선이 확실히 꽂히게 한다.

## 타이포그래피

- **본문/UI 전체**: Pretendard (한글+영문 겸용, 시스템 폰트보다 깔끔함). 없으면 `Segoe UI` / `-apple-system` 폴백
- **숫자/버전 표시 (모드 버전, 진행률 %)**: `JetBrains Mono` — 모노스페이스로 고정폭 처리해서 숫자 흔들림 방지
- 굵기는 Regular(400) / Medium(500) / SemiBold(600) 세 단계만 사용. Bold(700) 이상 안 씀
- 타이틀 22px SemiBold / 본문 14px Regular / 보조 텍스트 12px Regular / 버튼 라벨 14px Medium

## 레이아웃

```
┌─────────────────────────────────────┐
│  [로고/타이틀]      [다크모드 토글]    │  ← 상단바, 48px 높이
├─────────────────────────────────────┤
│                                     │
│         [계정 아바타 + 닉네임]        │  ← 좌측 사이드 or 상단 배치
│                                     │
│  ┌───────────────────────────────┐  │
│  │  모드 목록 / 버전 정보 카드      │  │
│  └───────────────────────────────┘  │
│                                     │
│  [============진행바 70%=========]  │  ← 다운로드 중일 때만 노출
│                                     │
│              [   플레이   ]         │  ← 유일한 라임 강조 버튼, 하단 고정
└─────────────────────────────────────┘
```

- 카드/패널: `border-radius: 10px`, 그림자 없음. 대신 1px `--border`로 구분
- 여백은 8px 배수로만 (8 / 16 / 24 / 32)
- 버튼 높이 40px, radius 8px. 보조 버튼은 outline 스타일 (배경 없음, `--border` 테두리)

## 시그니처 요소

**진행바**: 얇은 라임 바 하나로 다운로드/설치 상태를 보여주되,
퍼센트 숫자를 모노스페이스로 바 위에 겹쳐서 표시. 로딩 스피너나 애니메이션 아이콘 없이
이 진행바 하나가 "지금 뭔가 되고 있다"는 걸 전달하는 유일한 동적 요소.

## 다크/라이트 전환

- 토글은 상단바 오른쪽 아이콘 하나 (해/달 아이콘, 텍스트 라벨 없음)
- 전환 시 즉시 반영, 애니메이션 트랜지션은 150ms ease만 (색상 급변 방지, 그 이상은 과함)

## 금지 사항

- 그라데이션 배경, 네온 글로우, 과한 그림자 금지
- 라임 외 추가 강조색(파랑, 빨강 등) 도입 금지 — 에러 상태도 무채색 톤 안에서 아이콘/텍스트로만 구분
- 아이콘은 outline 스타일 통일 (filled/outline 혼용 금지)
