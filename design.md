# GreenLauncher 디자인 스펙

## 컨셉

심플, 절제된 톤. 라임 컬러 하나만 강조색으로 쓰고 나머지는 무채색으로 눌러준다.
장식 요소 최소화 — 사이드바 내비게이션과 카드 몇 종류로 구성되는 마인크래프트 런처답게
"조용한 도구" 느낌을 낸다. 화려한 그라데이션이나 네온 글로우는 여전히 금지하되,
카드에는 옅은 앰비언트 섀도우를 허용한다 (아래 "카드/버튼" 참고).

> 2026-08-02: Claude Design에서 가져온 새 목업(`Green Launcher.dc.html`)을 반영해
> 타이틀바 + 사이드바 + 5개 페이지(홈/서버 목록/스킨/설정/계정) 셸 구조로 전면 개편.
> 기존 420×640 단일 화면 스펙은 폐기.

## 컬러 토큰

### 라이트 모드

| 토큰                    | 값                     | 용도                            |
| ----------------------- | ---------------------- | ------------------------------- |
| `--bg`                  | `#FFFFFF`              | 콘텐츠 영역 배경                |
| `--titlebar-bg`         | `#F7F8F6`              | 타이틀바 배경                   |
| `--sidebar-bg`          | `#F7F8F6`              | 사이드바 배경                   |
| `--surface`             | `#FFFFFF`              | 카드, 패널                      |
| `--hero-bg`             | `#F2F7EA`              | 홈 히어로 카드 배경             |
| `--border`              | `rgba(0,0,0,.07)`      | 구분선, 테두리                  |
| `--text-primary`        | `#1A1B1E`              | 본문 텍스트                     |
| `--text-secondary`      | `#6B7280`              | 보조 텍스트                     |
| `--accent`              | `#89D22F`              | 라임 (버튼, 강조, 포커스)       |
| `--accent-text`         | `#12210A`              | 라임 배경 위 텍스트             |
| `--accent-tint`         | `rgba(138,210,48,.14)` | 활성 내비/뱃지 배경             |
| `--accent-text-on-tint` | `#4F7A1A`              | 라임 틴트 배경 위 텍스트/아이콘 |

### 다크 모드

| 토큰                    | 값                      | 용도                            |
| ----------------------- | ----------------------- | ------------------------------- |
| `--bg`                  | `#1A1B1E`               | 콘텐츠 영역 배경                |
| `--titlebar-bg`         | `#212226`               | 타이틀바 배경                   |
| `--sidebar-bg`          | `#1A1B1E`               | 사이드바 배경                   |
| `--surface`             | `#26272B`               | 카드, 패널                      |
| `--hero-bg`             | `#26272B`               | 홈 히어로 카드 배경             |
| `--border`              | `rgba(255,255,255,.08)` | 구분선, 테두리                  |
| `--text-primary`        | `#F0F0F0`               | 본문 텍스트                     |
| `--text-secondary`      | `#9CA3AF`               | 보조 텍스트                     |
| `--accent`              | `#89D22F`               | 라임 — 라이트와 동일 값         |
| `--accent-text`         | `#12210A`               | 라임 배경 위 텍스트 (동일)      |
| `--accent-tint`         | `rgba(138,210,48,.16)`  | 활성 내비/뱃지 배경             |
| `--accent-text-on-tint` | `#A9E35D`               | 라임 틴트 배경 위 텍스트/아이콘 |

**규칙**: 라임은 오직 하나 — 기본 액션(플레이/실행 버튼, 진행바 채움, 활성 내비 하이라이트)에만
쓴다. 이번 개편으로 라이트/다크 모두 동일한 `#89D22F` 한 값으로 통일했다 (이전엔 모드별로
살짝 다른 라임 값을 썼음). 아이콘, 보조 버튼, 카드 테두리 등 나머지는 전부 무채색 스케일로
처리해서 라임이 나타났을 때 시선이 확실히 꽂히게 한다. 에러/경고 상태도 여전히 무채색
톤 안에서 아이콘·텍스트로만 구분한다 — 예: 계정 페이지의 로그아웃 버튼은 원본 목업에서
빨강(`#c0392b`) 텍스트였지만, 이 규칙을 지키기 위해 무채색 아웃라인 버튼으로 유지한다.

## 타이포그래피

- **본문/UI 전체**: Pretendard (한글+영문 겸용). 없으면 `Segoe UI` / `-apple-system` 폴백.
  가져온 목업은 Manrope(구글 폰트)를 썼지만, 이 폰트는 한글 글리프가 없고 앱 UI 텍스트
  대부분이 한글이라 채택하지 않았다 — Pretendard 유지.
- **숫자/버전 표시 (모드 버전, 핑, 메모리 GB, 진행률 %)**: `JetBrains Mono` — 모노스페이스로
  고정폭 처리해서 숫자 흔들림 방지.
- 굵기는 Regular(400) / Medium(500) / SemiBold(600) 세 단계만 사용. Bold(700) 이상 안 씀.
- 크기 스케일 (컴포넌트별):
  - 홈 히어로 타이틀 22px SemiBold
  - 섹션 헤더 (서버 목록/스킨/설정) 18px SemiBold
  - 사이드바 내비 라벨 13px SemiBold
  - 카드 본문/타이틀 13–14px (SemiBold 강조 시 14px)
  - 보조/메타 텍스트 11–12px Regular
  - 버튼 라벨 13–16px Medium/SemiBold (CTA는 16px, 일반 버튼은 13px)

## 레이아웃

```
┌───────────────────────────────────────────────────────┐
│ [G] Green Launcher                                     │  ← 타이틀바, 44px, OS 네이티브 창 프레임 유지
├───────────┬───────────────────────────────────────────┤
│  ⌂ 홈      │                                           │
│  ≡ 서버 목록│         페이지별 콘텐츠 (아래 참고)          │
│  ▤ 스킨    │                                           │
│  ⚙ 설정    │                                           │
│  ◻ 계정    │                                           │
│           │                                           │
│  ────────  │                                           │
│  (아바타)   │                                           │
│  닉네임    │                                           │
│  다크 모드 ○│                                           │
└───────────┴───────────────────────────────────────────┘
   사이드바 240px         콘텐츠 (padding 40/32)
```

- **타이틀바 (44px)**: 좌측 로고 배지 + "Green Launcher"만 표시. 목업은 프레임리스 창을
  가정해 자체 최소화/최대화/닫기 아이콘을 그렸지만, 실제로는 OS 네이티브 창 프레임을
  유지한다 — 이미 실제로 동작하는 OS 버튼이 있는데 가짜 장식 아이콘을 또 그리는 건
  죽은 UI이기 때문. 창은 1280×800 기본, 리사이즈 가능, 최소 960×600.
- **사이드바 (240px)**: 홈/서버 목록/스킨/설정/계정 5개 내비 항목 + 하단에 계정 아바타/닉네임
  요약 (구분선으로 분리) + 그 바로 아래 다크/라이트 모드 토글. 사이드바는 로그인 성공 후
  메인 셸에서만 렌더링되므로, 로그인 화면에는 토글이 노출되지 않는다.
- **콘텐츠 영역**: 페이지별로 하나만 표시.
  - **홈**: 히어로 카드(버전 뱃지 + 타이틀 + 실행 버튼) → 진행바(다운로드/설치 중에만) →
    설치된 모드 목록 카드.
  - **서버 목록**: 서버 카드 리스트 (아이콘/이름/상태점/플레이어 수/핑/접속 버튼).
  - **스킨**: 큰 미리보기 패널 + 스킨 목록 (업로드 지원).
  - **설정**: 메모리 할당 슬라이더, Java 경로/게임 디렉토리 표시.
  - **계정**: 프로필 카드, 연결된 계정 상태, 로그아웃.
- 여백은 8px 배수로만 (8 / 16 / 24 / 32 / 40).

## 카드/버튼

- **카드**: 그림자 없음 규칙은 폐기 — 옅은 앰비언트 섀도우 허용.
  - 라이트: `0 1px 3px rgba(0,0,0,.06), 0 6px 16px rgba(0,0,0,.04)`
  - 다크: `0 2px 10px rgba(0,0,0,.35)`
  - 네온 글로우, 과하게 퍼지는 그림자는 여전히 금지 — 카드 구분이 목적인 최소한의
    깊이감만 허용한다.
  - 코너 라디우스는 컴포넌트별: 히어로 카드 20px, 일반 리스트/패널 카드 14px,
    작은 뱃지/태그/버튼은 pill(999px).
- **버튼**: 기본/CTA/아웃라인 버튼 모두 pill(999px) 라디우스로 통일 (이전 8px 규칙 폐기).
  Primary CTA(실행 버튼)는 라임 배경 + `--accent-text`. 보조 버튼은 배경 없음 +
  `--border` 아웃라인, 텍스트는 `--text-primary`.

## 시그니처 요소

**진행바**: 얇은 라임 바 하나로 다운로드/설치 상태를 보여주되, 퍼센트 숫자를
모노스페이스로 바 위에 겹쳐서 표시. 로딩 스피너나 애니메이션 아이콘 없이 이 진행바
하나가 "지금 뭔가 되고 있다"는 걸 전달하는 유일한 동적 요소. 홈 페이지의 히어로 카드와
설치된 모드 목록 사이에 위치하며, 유휴 상태에는 완전히 숨긴다.

## 다크/라이트 전환

- 기본값(최초 실행)은 **라이트 모드**.
- 토글은 사이드바 하단, 계정 아바타/닉네임 요약 바로 아래에 위치하는 스위치
  (트랙 36×20px, 썸 16px, 좌 2px / 우 18px). 로그인 화면에는 표시되지 않고,
  로그인 성공 후 메인 셸에서만 접근 가능하다.
  > 2026-08-03: 기존엔 타이틀바 우측 단독 배치라 아이콘/라벨 없이 트랙·썸 위치만으로
  > 상태를 표현했다. 사이드바로 옮기면서 나머지 계정 요약 텍스트들과 나란히 놓이는
  > 리스트형 항목이 되어, 라벨 없이는 무엇을 위한 스위치인지 문맥이 약해진다고 판단해
  > "다크 모드" 텍스트 라벨을 왼쪽에 추가했다 (13px SemiBold, 본문 텍스트와 동일 톤 —
  > 새 강조색 도입 없음). 트랙/썸 자체의 무라벨 규칙은 폐기.
- 전환 시 즉시 반영, 배경/썸 위치 트랜지션은 150ms ease만 (색상 급변 방지, 그 이상은 과함).

## 금지 사항

- 그라데이션 배경, 네온 글로우, 과한 그림자 금지 (카드용 옅은 앰비언트 섀도우는 예외로 허용).
- 라임 외 추가 강조색(파랑, 빨강 등) 도입 금지 — 에러 상태도 무채색 톤 안에서 아이콘/텍스트로만
  구분.
- 아이콘은 outline 스타일 통일 (filled/outline 혼용 금지).
- 실제로 동작하지 않는 장식용 컨트롤(가짜 창 버튼, 가짜 편집 가능 필드 등) 추가 금지 —
  기능이 없으면 읽기 전용/비활성 상태로 명확히 표시할 것.
